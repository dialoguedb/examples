# Metadata — llm-exe + DialogueDB Episode
Series: Stateful (flagship) | Example: examples/llm-exe

---

## Title Options (SEO-first)

1. "Give llm-exe Pipelines Persistent Memory (in 7 minutes)"
2. "Your Typed LLM Pipeline Forgets Users. Here's the Fix."
3. "Persist llm-exe Conversations and Metadata Across Restarts"
4. "llm-exe + DialogueDB: Typed Pipelines That Remember"
5. "Add Memory to llm-exe Chatbots — Survives Restarts"

---

## Thumbnail Concept

**Claim text:** TYPED PIPELINES THAT REMEMBER
**Visual:** split screen — left side shows a terminal with structured JSON output (`{ response, intent, sentiment }`) and a red "process killed" overlay; right side shows a fresh terminal with the same conversation restored in green. llm-exe logo in the corner, DialogueDB mark bottom-right. High contrast dark background, monospace type, readable at phone size.

---

## YouTube Description

```
Your llm-exe pipeline extracts structured output on every LLM call — response, intent, sentiment, all typed. But it's in-memory. Process dies, everything's gone.

In this episode, we build a travel chatbot with llm-exe's executor pipeline and persist every message — plus its structured metadata — to DialogueDB. Kill the process, start a new one, and the bot still knows your trip, your diet, and your itinerary.

Full code: https://github.com/dialoguedb/examples/tree/main/llm-exe
Free API key: https://dialoguedb.com
Docs: https://docs.dialoguedb.com
npm: https://npmjs.com/package/dialogue-db
llm-exe: https://llm-exe.com

---

Chapters:
0:00 Cold open — the restart that remembers
0:15 The problem with in-memory LLM state
0:50 The plan
1:15 Move 1 — Building the typed executor pipeline
2:30 Move 2 — Persisting messages with structured metadata
3:50 Move 3 — Multi-turn conversation
5:15 Money shot — cold resume from a new process
6:15 The pattern + what's next

---

Stateful is a series about giving AI apps permanent memory. Each episode builds one runnable example, start to finish. Clone the repo and follow along.

https://x.com/dialoguedbapp
```

---

## Tags

1. llm-exe
2. llm-exe tutorial
3. AI conversation memory
4. LLM pipeline persistence
5. DialogueDB
6. typed LLM pipeline
7. chatbot memory
8. AI app state management
9. structured LLM output
10. conversation persistence
11. LLM executor pipeline
12. AI chatbot tutorial
